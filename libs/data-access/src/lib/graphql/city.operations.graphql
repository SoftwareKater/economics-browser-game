query cities {
  cities {
    id
    uuid
    name
  }
}

query getMyCity {
  getMyCity {
    id
    name
    habitants {
      id
      name
      employment {
        id
        name
      }
      starving
      accommodation {
        id
        name
      }
    }
    developments {
      buildingId
    }
  }
}

mutation createCity($name: String!) {
  createCity(name: $name) {
    id
    name
  }
}
